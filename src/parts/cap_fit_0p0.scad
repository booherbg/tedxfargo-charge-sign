// Fit test: over-cap skirt clearance 0.0mm (3 dots). Tightest — likely a snap
// once print tolerance is added.
include <../config.scad>
include <../collar.scad>
include <../bolt.scad>
cap_chunk(0.0, 3);
