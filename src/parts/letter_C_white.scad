// Letter C tile — WHITE liner + inner walls + collars (filament 2).
include <../config.scad>
include <../collar.scad>
include <../letter.scad>
include <letter_C_data.scad>
letter_white(C_paths, C_closed, C_pixels, C_bbox);
