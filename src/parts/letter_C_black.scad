// Letter C tile — BLACK base + outer walls (filament 1).
include <../config.scad>
include <../collar.scad>
include <../letter.scad>
include <letter_C_data.scad>
letter_black(C_paths, C_closed, C_pixels, C_bbox, "C");
