// CLEAR integrated lens with a BAKED fuzzy (random bumpy) top. Print lens-up. filament 2.
// Needs fuzz.dat in this dir (tools/make_fuzz.py). Pair with testbox_white for the 3MF.
include <../config.scad>
include <../collar.scad>
include <../testbox.scad>
testbox_clear_fuzzy();
